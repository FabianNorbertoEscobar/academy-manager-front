import { HeaderFont } from './header-font';

describe('HeaderFont', () => {
  it('should create an instance', () => {
    const directive = new HeaderFont();
    expect(directive).toBeTruthy();
  });
});
